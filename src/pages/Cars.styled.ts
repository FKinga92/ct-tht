import {styled} from 'styled-components';

export const CarsSC = styled.div`
  padding: 0 1.785em;

  .car-list-container {
    margin: 1.428em 0;
    display: flex;
    flex-wrap: wrap;
    gap: 1.0714em;
    justify-content: space-between;
  }
`;
